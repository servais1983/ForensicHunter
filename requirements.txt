# ForensicHunter — core dependencies
# Install with: pip install -r requirements.txt

# CLI / output
click>=8.1.3
rich>=13.3.5
tqdm>=4.65.0
colorama>=0.4.6
tabulate>=0.9.0

# System info
psutil>=5.9.5

# YARA threat detection
yara-python>=4.3.1

# Windows artefact parsing (Windows only)
python-registry>=1.3.1; platform_system=="Windows"
pywin32>=306;           platform_system=="Windows"
wmi>=1.5.1;             platform_system=="Windows"
python-magic-bin>=0.4.14; platform_system=="Windows"
python-magic>=0.4.27;    platform_system!="Windows"

# Memory forensics
volatility3>=2.4.1

# PE / binary analysis
pefile>=2023.2.7
pyelftools>=0.29
capstone>=4.0.2

# Event log parsing
python-evtx>=0.7.4

# Document / data parsing
lxml>=4.9.2
jinja2>=3.1.2
pandas>=2.0.1

# Visualisation
matplotlib>=3.7.1
networkx>=3.1

# HTTP / async
requests>=2.31.0
aiohttp>=3.8.4

# Image processing
pillow>=9.5.0

# Browser history
browserhistory==0.1.2
pycryptodome>=3.18.0

# ML anomaly detection
scikit-learn>=1.2.2
scipy>=1.10.1

# Memory introspection
pymem>=1.12.0
construct>=2.10.68

# Configuration / env
pyyaml>=6.0.1
python-dotenv>=1.0.0

# GUI (Windows only — optional)
PyQt5>=5.15.9; platform_system=="Windows"

# API server (optional — install requirements-prod.txt for API)
# fastapi, uvicorn, pydantic, structlog — see requirements-prod.txt

# Testing
pytest>=7.3.1
pytest-cov>=4.1.0
